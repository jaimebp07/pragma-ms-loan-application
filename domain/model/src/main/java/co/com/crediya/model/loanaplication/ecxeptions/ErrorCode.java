package co.com.crediya.model.loanaplication.ecxeptions;

public enum ErrorCode {
    VALIDATION_ERROR,
    CLIENT_NOT_FOUND,
    DB_ERROR,
    UNEXPECTED_ERROR,
    INVALID_ARGUMENT,
    VALUE_REQUIRED,
    INVALID_FORMAT
}
