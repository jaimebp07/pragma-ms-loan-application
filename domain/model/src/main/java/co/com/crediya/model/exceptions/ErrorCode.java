package co.com.crediya.model.exceptions;

public enum ErrorCode {
    VALIDATION_ERROR,
    CLIENT_NOT_FOUND,
    LOAN_TYPE_NOT_FOUND,
    DB_ERROR,
    UNEXPECTED_ERROR,
    INVALID_ARGUMENT,
    VALUE_REQUIRED,
    INVALID_FORMAT,
    UNAUTHORIZED
}
